version: "3.1"
services:
  checkout:
    image: checkout
    build: ./checkout/
    ports:
      - "50050:50050"
  loms:
    image: loms
    build: ./loms/
    ports:
      - "50051:50051"
  # notification:
  #   image: notification
  #   build: ./notification
  #   ports:
  #     - "8082:8082"
